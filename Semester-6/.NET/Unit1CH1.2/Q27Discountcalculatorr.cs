#if false
using System;

namespace Unit1CH1._2
{
    internal class DiscountCalculatorr
    {
        /*
        static void Main()
        {
            Console.Write("Enter quantity purchased: ");
            int quantity = int.Parse(Console.ReadLine());

            Console.Write("Enter price per item: ");
            double price = double.Parse(Console.ReadLine());

            double total = quantity * price;

            if (quantity > 1000)
            {
                double discount = total * 0.10;
                total -= discount;
                Console.WriteLine($"\n10% discount applied: Rs.{discount:F2}");
            }

            Console.WriteLine($"Total expenses: Rs.{total:F2}");
            Console.ReadKey();
        }
        */
    }
}

#endif

