#if false
using System;

namespace Unit1CH1._2
{
    internal class RatioCalculator
    {
        /*
        static void Main()
        {
            Console.Write("Enter value of a: ");
            double a = double.Parse(Console.ReadLine());

            Console.Write("Enter value of b: ");
            double b = double.Parse(Console.ReadLine());

            Console.Write("Enter value of c: ");
            double c = double.Parse(Console.ReadLine());

            Console.Write("Enter value of d: ");
            double d = double.Parse(Console.ReadLine());

            double denominator = c - d;

            if (denominator != 0)
            {
                double ratio = (a + b) / denominator;
                Console.WriteLine($"\n(a+b)/(c-d) = ({a}+{b})/({c}-{d}) = {ratio:F2}");
            }
            else
            {
                Console.WriteLine("\nError: Denominator (c-d) is zero! Cannot divide by zero.");
            }

            Console.ReadKey();
        }
        */
    }
}

#endif

